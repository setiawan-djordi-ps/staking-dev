import React from 'react'
import styles from "./staking-title.module.scss";

function StakingTitle() {
  return (
    <section className={styles["staking-title-container"]}>
        <h1>Staking</h1>
    </section>
  )
}

export default StakingTitle;